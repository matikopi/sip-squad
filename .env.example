# Optional. If set, each photo is sent to Claude to estimate the cup size.
# If unset, each person's default cup size is used instead.
ANTHROPIC_API_KEY=

# Optional overrides. Defaults point at the Supabase project the app was built with.
# SUPABASE_URL=
# SUPABASE_PUBLISHABLE_KEY=
